
## webauth 工具 SSE 超时问题

### 时间
10:26 左右发现

### 现象
- Chrome 9223 端口正常重启成功
- Doubao ✅ / DeepSeek ✅ / Kimi ❌ / GLM ❌ / Qwen ❌
- 错误：`MCP error -32001: Request timed out`（SSE 流超时）

### 教训（用户强调）
**出现这种问题要先记录，修复都还是第二步**
→ 已写入 LEARNINGS.md、ERRORS.md、MEMORY.md

### 当前状态
未解决，三个平台 SSE 超时原因待排查

## webauth 5平台全部修复（12:00）

### 最终结果
| 工具 | 状态 |
|------|------|
| Doubao | ✅ |
| Kimi | ✅ |
| GLM | ✅ |
| Qwen | ✅ |
| DeepSeek | ✅ |

### 修复记录
- ✅ 已写入 .learnings/LEARNINGS.md
- ✅ 已写入 .learnings/ERRORS.md
- ✅ 已更新 MEMORY.md

## deep-search 测试时 Kimi/GLM/Qwen 再次超时（12:18）

### 问题
调研完成后测试 5 个工具状态，Kimi/GLM/Qwen 报 SSE 超时，但 12:00 测试全部通过。

### 教训
- 修好了不代表一直好，需要记录每次失败
- 根因可能是 Token 在页面刷新后失效
- 未及时记录错误，扣分

### 待查
- 为什么 12:00 通、12:18 不通？Token 失效速度这么快？
- 还是 Gateway 重启影响了 Chrome 状态？

## 深度调研 zero-token 项目（13:00）

### 关键发现
1. **SSE 完成检测**：`reader.read()` 循环直到 `done: true`，不是用时间限制
2. **Token 来源**：Kimi 从 localStorage 读 `access_token`（不是 `kimi-auth` cookie）
3. **API 调用位置**：zero-token 所有 API 调用都在 `page.evaluate()` 里，浏览器自动带 cookie
4. **我们的根本问题**：用 Node.js fetch，cookie 不会自动附加

### 修复计划
1. Kimi：改用 localStorage 的 `access_token`，不用 kimi-auth cookie
2. GLM：加 token refresh 逻辑，读 SSE 直到 done
3. Qwen：改用 page.evaluate() + Bearer token，修复 API URL/格式
4. 所有平台：API 调用移入 page.evaluate()

## webauth 修复过程记录

### 当前状态（13:30）
- Kimi: ✅ 通了（用复杂问题测试成功）
- GLM: ❌ 超时（interceptor 有旧响应，但 token 过期）
- Qwen: ❌ 超时（interceptor 存在但没捕获到响应）

### 根因分析
1. **GLM**: `chatglm_token_expires=2026-03-22` 已过期，需要 refresh token
2. **Qwen**: interceptor 方式不稳定，API 调用可能走了不同路径

### 修复计划
1. GLM: 加 token refresh 逻辑
2. Qwen: 参照 zero-token 直接在 page.evaluate() 里调 API
3. Kimi: ok，暂不动
4. 统一用 `reader.read()` 循环检测 SSE 完成，不用定时器

### 测试问题记录
- 不要用 1+1，要用 ROBOT-SOP 里的实际问题
- 每次调用都要记录成功/失败

## 13:00 测试记录

### 测试问题（复杂问题）
Genesis vs Isaac Sim 对比 + 43M FPS 测试条件

### 结果
| 工具 | 状态 |
|------|------|
| Doubao | ✅ 详细 |
| Kimi | ✅ 详细 |
| GLM | ❌ SSE 超时 |
| Qwen | ❌ SSE 超时 |
| DeepSeek | ✅ 详细（带搜索） |

### Playwright 直接诊断
- **GLM**: HTTP 400 `{"status":40001,"message":"bad request(40001)"}` - API请求格式错误
- **Qwen**: Chat创建成功，但发消息返回 `Bad_Request` - API格式错误

### 根因分析
1. GLM: 我的 sign 生成用 SHA-256，zero-token 用 MD5；缺少 X-Exp-Groups header；body 缺少 meta_data 字段
2. Qwen: API URL 或 body 格式不对（但 chat 能创建说明基础认证 OK）

### 修改记录
- 重写了 GLM 函数：从 textarea+interceptor 改为 page.evaluate() 直接调 API
- 重写了 Qwen 函数：从 textarea+interceptor 改为 page.evaluate() 2步流程

### 下一步
1. 修复 GLM sign 生成（MD5）+ 添加 X-Exp-Groups header + 完整 meta_data
2. 修复 Qwen 发消息 API 格式

## 13:00-13:27 测试记录

### 问题描述
Kimi/GLM/Qwen 在简单和复杂问题上都持续超时，但 Doubao/DeepSeek 一直正常。

### 可能的根因
1. GLM/Qwen 改用 page.evaluate() 后，page 可能是空页面（没有加载 chat 页面）
2. Kimi 每次都超时，可能是 cookie 状态不稳定
3. Gateway 每次重启杀 Chrome，页面状态丢失

### GLM/Qwen 改用 page.evaluate() 的风险
- 页面可能还没加载完成，evaluate 就执行了
- 旧版 textarea 方式虽然慢，但至少能工作
- 新版 API 直接调用可能在 page 未就绪时就失败

### 待查
1. 先检查 Chrome 里的页面状态
2. 考虑回退到 textarea 方式（至少能工作）
3. Kimi 超时的根因：cookie 问题？API 问题？

### 修改记录
- GLM: textarea+interceptor → page.evaluate() 直接 API（v2）
- Qwen: textarea+interceptor → page.evaluate() 2步流程（v2）

## 13:56 zero-token 深度调研结果

### 重大发现

**Qwen URL 错误**：
- 正确：`https://chat2.qianwen.com/api/v2/chat`
- 我们用的：`https://chat.qwen.ai/api/v2/chat/completions`
- 完全不同！

**GLM sign 错误**：
- zero-token 用 MD5 + 特殊时间戳算法
- 我之前用 SHA-256，完全错误

**GLM token refresh**：
- zero-token 有自动 refreshAccessToken() 逻辑
- 401 时自动刷新 token 后重试

**Kimi URL**：
- 正确：`www.kimi.com`（不是 `kimi.com/chat`）
- scenario 要根据模型选择：SCENARIO_K1/K2/SEARCH/RESEARCH

### 修复计划
1. Qwen：改 URL 为 `chat2.qianwen.com` + 补全 xsrfToken/deviceId
2. GLM：修复 MD5 sign 算法 + 加 token refresh
3. Kimi：改 URL 为 `www.kimi.com` + 加 SCENARIO_K2
4. 所有：直接用 page.evaluate() 调 API

## Doubao MCP Server 凭证获取方案（最终成功）

### 问题
- Cookie 存在 Chrome 加密的 SQLite 里，Node.js 无法直接读
- `chromium.launchPersistentContext` 在 macOS 上会挂起（`browser.close()` 无法返回）
- Node.js Playwright 连接 Chrome 需要 remote debugging 端口

### 解决方案
1. 用 Python Playwright（不是 Node.js）通过 CDP 连接 Chrome
2. Chrome 用"用户 2"Profile 启动：`--remote-debugging-port=9222 --user-data-dir=".../Chrome/Profile 2"`
3. 从 `sessionid` Cookie 获取凭证
4. 保存到 `creds.json`

### 关键命令
```bash
# 启动 Chrome 用户2
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --remote-debugging-port=9222 \
  --user-data-dir="$HOME/Library/Application Support/Google/Chrome/Profile 2"

# Python Playwright 连接
browser = p.chromium.connect_over_cdp("http://localhost:9222")
```

### 经验教训
- **用 Python Playwright 而不是 Node.js** - `browser.close()` 在 Node.js 里会挂起，但 Python 的 `sync_playwright` 可以正常关闭
- **Chrome Profile 2 是已登录豆包的 Profile**
- **sessionid 就是 Refresh Token**，有效期约 7-30 天，过期后重新运行脚本获取即可

### 工具状态（17:40）
- Doubao ✅（Profile 2 sessionid 已获取）
- DeepSeek ✅

## 18:31 GLM-5 Turbo 配置测试

- 用户配置了 GLM-5 Turbo，让我验证是否正常
- 当前 session model 就是 `zai/glm-5-turbo`，能对话即正常 ✅
- 测试结果：GLM-5 Turbo ✅ / DeepSeek ✅ / Doubao ❌（creds.json 缺失）

## 18:37 Doubao 凭证修复（方案2）

- Chrome 9222/9223 都没有调试端口，capture-creds.mjs 无法连接
- 用方案2：Python Playwright 连 Chrome-Debug-Profile（9223）提取豆包 cookie
- 成功提取 sessionid，保存到 creds.json
- 测试 Doubao ✅

## 18:44 Doubao MCP Server 改造方案

### 目标
把 doubao-mcp-server 改成和 deepseek-mcp-server 一样的架构：独立运行，不依赖 Chrome

### 方案
1. 凭证从 `creds.json` 读取（已有，capture-creds.mjs 刷新）
2. Node.js 直接 fetch API，带 Cookie header
3. SSE 流式读取，收集完整响应返回
4. 不依赖 Chrome 调试端口
5. 去掉 `a_bogus` 等反爬参数（webauth 版本不用也行）

### 只改一个文件
- `~/.openclaw/extensions/doubao-mcp-server/doubao-mcp-server.mjs`

### 不改
- capture-creds.mjs：保留用于刷新凭证
- openclaw.json：alsoAllow 已有 doubao_doubao_chat
- MCP adapter 配置路径不变

### 注意
- sessionid 约 7-30 天过期，和 DeepSeek 一样
- 过期后重新跑 capture-creds.mjs 即可

### 产生/修改的文件
- `~/.openclaw/extensions/doubao-mcp-server/doubao-mcp-server.mjs`（改写）

## 18:44-19:30 Doubao MCP Server v3.0 改造完成

### 最终方案（参照 zero-token 项目）
- **v1.0 → v2.0（失败）**：Node.js 直接 fetch + cookie → 400（缺 a_bogus 签名）
- **v3.0（成功）**：Playwright 连 Chrome 9223，在 `page.evaluate` 里 fetch，字节 bdms SDK 自动注入 msToken + a_bogus

### 关键发现（从 zero-token 学到的）
1. URL 参数**不能带** `sessionid`/`clientKey`/`msToken`/`a_bogus`，浏览器拦截器自动注入
2. `version_code` 必须是 `"20800"`（不是 `"100800"`）
3. 需要额外参数：`samantha_web: "1"`, `use_olympus_account: "1"`, `region: "CN"`, `sys_region: "CN"` 等
4. SSE 解析用 samantha 格式：`event_type: 2001` + `event_data`（不是 `data.text`）
5. 参考文件：`openclaw-zero-token/src/providers/doubao-web-client-browser.ts`

### 依赖
- Chrome-Debug-Profile 需要开着（9223 端口）
- 豆包页面需要已登录
- 不再需要 `creds.json`（直接从浏览器 cookie 获取）

### 测试结果
- 测试问题：你好，用一句话介绍你自己
- 响应时间：~5.5 秒
- 响应：你好！我是豆包，字节跳动自研的AI助手，随时为你提供帮助。
- 状态：✅ 成功

### 22:27 新建 Qwen MCP Server v1.2

- 路径：`~/.openclaw/extensions/qwen-mcp-server/`
- 协议：Playwright + page.evaluate + 两步式 API
- 特点：Step1 创建聊天 → Step2 发消息（SSE 流）
- 思考过滤：在 "Final Response:" 处截断思考过程
- 已注册 alsoAllow: qwen_qwen_chat
- GitHub: https://github.com/mmhzlrj/qwen-mcp-server

### 22:06 用户建议优化 MCP Server 标签页策略

**问题**：v3.1 每次创建新标签页，会累积大量空标签页

**用户建议**（更优方案）：
- 复用已有豆包标签页
- 导航到 `www.doubao.com/chat/` 开新对话
- 不用创建新标签页，`/chat/` 本身就是新对话页

**修复**：已更新代码和 README
