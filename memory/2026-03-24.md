# 2026-03-24 工作日志

## 今日任务
- [ ] mdview.py 审批模式开发（subagent 进行中）

## 11:41 mdview.py CSS 修复

### 🐛 错误
- **现象**：审批模式 HTML 页面 CSS 被当作纯文本显示在页面上
- **根因**：CSS 变量（`BASE_CSS`、`REVIEW_CSS`）直接用 f-string 插入到 `<head>` 中，没有 `<style>` 标签包裹
- **两处遗漏**：
  1. 普通模式 `generate_normal_html()`：`{BASE_CSS}` 无 `<style>` 包裹
  2. 审批模式 `generate_review_html()`：`{BASE_CSS}` 和 `{REVIEW_CSS}` 无 `<style>` 包裹

### ✅ 修复
- 第1处：`{BASE_CSS}` → `<style>{BASE_CSS}</style>`
- 第2处：`{BASE_CSS}\n{REVIEW_CSS}` → `<style>{BASE_CSS}\n{REVIEW_CSS}</style>`
- 注意：`{REVIEW_JS}` 已经自带 `<script>` 标签，不需要额外包裹

### 💡 教训
1. **f-string 模板中的 CSS/JS 必须检查标签包裹**：用变量插入时容易忘记外层 HTML 标签
2. **旧代码有 `<style>` 包裹是因为写死在字符串里**：重构为变量后丢失了包裹
3. **应该 diff 检查**：重构后用 diff 对比新旧输出，确认结构完整

### 产生/修改的文件
- `tools/mdview.py` — 修复 CSS `<style>` 包裹 + 新增审批模式（完整重构）
- commit: `1748b46` fix(mdview): 修复 CSS 未被 <style> 标签包裹导致样式失效的问题
- golden 备份已更新：`golden_20260324_114128`

---

## 11:57-13:09 Deep Research 工具检查 SOP 开发

### 背景
MiniMax 在 deep-research 任务中反复犯同一个错误：用 Playwright 打开 5 个 AI 网页版检查"登录态"，而不是直接调用 webauth 工具本身验证可用性。

### 产生/修改的文件

#### 1. `.learnings/deep-research-tool-check-SOP.md`（新建）
- 完整的工具状态检查 SOP
- 5 步流程：Chrome CDP → 5 个 webauth 工具 → 搜索工具 → subagent → 记录结果
- 常见错误对照表（7 条）
- mdview 正确操作流程

#### 2. `.learnings/deep-research-usage-guide.md`（更新）
- 第一步完全重写：从"打开页面检查"改为"直接调用工具"
- 新增 mdview --review 联动流程（5 步审批链：生成 MD → 打开 → 用户审批 → 读 JSON → 应用修改）

#### 3. `skills/deep-research/SKILL.md`（更新）
- 工具使用建议表格更新（minimax search 替代 Brave Search）
- 新增工具检查规范引用

#### 4. `skills/git-auto-push/SKILL.md`（新建）
- git push 失败时的自动重试 skill
- 三种方式：立即重试 / cron 定时重试 / 故障排除表

#### 5. `tools/mdview.py`（修复）
- **防重复标签页**：`ps aux` 检测已有进程，有则复用不启动新的
- **防僵尸进程**：`threading.Event` + `os._exit(1)` 强制退出
- **SO_REUSEADDR**：防止 TIME_WAIT 端口冲突

### 关键 commit
- `322607a` feat: 新增 deep-research 工具检查 SOP
- `cd50d14` fix: SOP 增加 mdview 重复标签页错误 + 端口占用正确处理流程
- `eab2781` fix: 工具检查 SOP 改用 thinking=true
- `c7d2be3` feat: 补充 mdview --review 联动流程
- `0a40214` fix(mdview): 检测已有进程防止重复打开标签页
- `1d1c695` fix(mdview): 防止僵尸进程 - Event 通知 + 服务验证 + os._exit

### 教训
1. **工具状态检查要直接调用工具本身**，不是打开页面看能不能加载
2. **thinking=true** 统一所有平台，不要省 token 用 thinking=false
3. **遇到报错先验证再行动**，不要慌张重复尝试
4. **先想最简方案再动手**，最终有效方案往往很简单（ps aux 检测进程）
5. **daemon 线程中 sys.exit 无效**，必须用 os._exit 强杀整个进程
