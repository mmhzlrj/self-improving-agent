# 2026-03-24 工作日志

## 今日任务
- [ ] mdview.py 审批模式开发（subagent 进行中）

## 11:41 mdview.py CSS 修复

### 🐛 错误
- **现象**：审批模式 HTML 页面 CSS 被当作纯文本显示在页面上
- **根因**：CSS 变量（`BASE_CSS`、`REVIEW_CSS`）直接用 f-string 插入到 `<head>` 中，没有 `<style>` 标签包裹
- **两处遗漏**：
  1. 普通模式 `generate_normal_html()`：`{BASE_CSS}` 无 `<style>` 包裹
  2. 审批模式 `generate_review_html()`：`{BASE_CSS}` 和 `{REVIEW_CSS}` 无 `<style>` 包裹

### ✅ 修复
- 第1处：`{BASE_CSS}` → `<style>{BASE_CSS}</style>`
- 第2处：`{BASE_CSS}\n{REVIEW_CSS}` → `<style>{BASE_CSS}\n{REVIEW_CSS}</style>`
- 注意：`{REVIEW_JS}` 已经自带 `<script>` 标签，不需要额外包裹

### 💡 教训
1. **f-string 模板中的 CSS/JS 必须检查标签包裹**：用变量插入时容易忘记外层 HTML 标签
2. **旧代码有 `<style>` 包裹是因为写死在字符串里**：重构为变量后丢失了包裹
3. **应该 diff 检查**：重构后用 diff 对比新旧输出，确认结构完整

### 产生/修改的文件
- `tools/mdview.py` — 修复 CSS `<style>` 包裹 + 新增审批模式（完整重构）
- commit: `1748b46` fix(mdview): 修复 CSS 未被 <style> 标签包裹导致样式失效的问题
- golden 备份已更新：`golden_20260324_114128`

---

## 11:57-13:09 Deep Research 工具检查 SOP 开发

### 背景
MiniMax 在 deep-research 任务中反复犯同一个错误：用 Playwright 打开 5 个 AI 网页版检查"登录态"，而不是直接调用 webauth 工具本身验证可用性。

### 产生/修改的文件

#### 1. `.learnings/deep-research-tool-check-SOP.md`（新建）
- 完整的工具状态检查 SOP
- 5 步流程：Chrome CDP → 5 个 webauth 工具 → 搜索工具 → subagent → 记录结果
- 常见错误对照表（7 条）
- mdview 正确操作流程

#### 2. `.learnings/deep-research-usage-guide.md`（更新）
- 第一步完全重写：从"打开页面检查"改为"直接调用工具"
- 新增 mdview --review 联动流程（5 步审批链：生成 MD → 打开 → 用户审批 → 读 JSON → 应用修改）

#### 3. `skills/deep-research/SKILL.md`（更新）
- 工具使用建议表格更新（minimax search 替代 Brave Search）
- 新增工具检查规范引用

#### 4. `skills/git-auto-push/SKILL.md`（新建）
- git push 失败时的自动重试 skill
- 三种方式：立即重试 / cron 定时重试 / 故障排除表

#### 5. `tools/mdview.py`（修复）
- **防重复标签页**：`ps aux` 检测已有进程，有则复用不启动新的
- **防僵尸进程**：`threading.Event` + `os._exit(1)` 强制退出
- **SO_REUSEADDR**：防止 TIME_WAIT 端口冲突

### 关键 commit
- `322607a` feat: 新增 deep-research 工具检查 SOP
- `cd50d14` fix: SOP 增加 mdview 重复标签页错误 + 端口占用正确处理流程
- `eab2781` fix: 工具检查 SOP 改用 thinking=true
- `c7d2be3` feat: 补充 mdview --review 联动流程
- `0a40214` fix(mdview): 检测已有进程防止重复打开标签页
- `1d1c695` fix(mdview): 防止僵尸进程 - Event 通知 + 服务验证 + os._exit

### 教训
1. **工具状态检查要直接调用工具本身**，不是打开页面看能不能加载
2. **thinking=true** 统一所有平台，不要省 token 用 thinking=false
3. **遇到报错先验证再行动**，不要慌张重复尝试
4. **先想最简方案再动手**，最终有效方案往往很简单（ps aux 检测进程）
5. **daemon 线程中 sys.exit 无效**，必须用 os._exit 强杀整个进程

---

## 13:09-16:20 ROBOT-SOP 全文档系统性调研 + Medusa Halo调研

### ROBOT-SOP 全文档调研（4批）

#### 第一批：硬件价格/规格验证（15个claim）
- ✅ NemoClaw 官方确认（GTC 2026-03-16）✅ Jetson Thor Nano不存在
- ✅ RTX 5050 9GB/336GB/s ✅ RTX 5050 ~2099元
- ⚠️ DGX Spark价格：$699→$4,699/34,999元（你查的京东自营）
- ⚠️ DGX Station 70万无据可查，保留原文
- ⚠️ BearPi H3863 GPIO：40针→17个GPIO（40针扩展排针座）
- ⚠️ Genesis vs Isaac Gym名称修正（4处）
- ⚠️ Genesis版本日期：v0.4.0（2026-02-17）
- ⚠️ AMD AI Halo未公布价格，整机参考17,999元（你拒绝了此修改）

#### 第二批：技术声明验证（13个claim）
- ✅ MQTT QoS1 <50ms ✅ WebSocket <20ms ✅ UART Pin正确 ✅ OV2640参数 ✅ CyberBricks型号
- ❌ Jetson Nano I2C Pin 3(SDA)/5(SCL)（原来SCL/SDA颠倒）
- ⚠️ vLLM Docker镜像名：vllm/vllm-openai:v0.14.0-rocm
- ⚠️ SGLang命令（误报，SOP原本就是对的）
- ❌ micro-ROS agent传输类型：wifi→udp4（2处）

#### 第三批：法规/新发现
- 💡 新发现：GB/T 40013-2021《家用和类似用途服务机器人》推荐性国标，已补充到第九章

#### 审批模式流程确认
- 每条修改必须有5字段：原文/建议改为/来源/优先级/状态
- mdview --review 打开审批模式
- 用户导出JSON后告知路径，AI读取并应用
- 教训：不能偷懒只靠subagent，主会话也要直接并行调用所有工具

### 审批结果应用汇总
| # | 内容 | 结果 |
|---|------|------|
| 1 | DGX Spark 34,999元 | ✅ 采纳 |
| 2 | DGX Station 保留 | ✅ 采纳 |
| 3 | BearPi 17个GPIO | ✅ 采纳 |
| 4 | RTX 5050 位宽96-bit | ✅ 采纳 |
| 5 | Isaac Gym（4处）| ✅ 采纳 |
| 6 | Genesis 版本v0.4.0 | ✅ 采纳 |
| 7 | AMD AI Halo 价格 | ❌ 拒绝 |
| 8 | I2C Pin 3(SDA)/5(SCL) | ✅ 采纳 |
| 9 | vLLM Docker镜像 | ✅ 采纳 |
| 10 | SGLang命令 | ❌ 误报无修改 |
| 11 | micro-ROS udp4 | ✅ 采纳 |

### Medusa Halo 调研
**背景**：用户想找一款移动终端，与家里AMD AI Halo（Strix Halo 128GB）遥相呼应，外出时完整展示0-1 AI能力

**核心发现**：
- Medusa Halo = AMD APU内部代号，对应Ryzen AI MAX 500系列
- 规格：Zen 6（24+2核）、RDNA 5（48 CU）、LPDDR6（460-691 GB/s）、256GB+统一内存
- 上市时间：2027年下半年~2028年
- 256GB LPDDR6：405B FP16可完整加载，8-12个Agent并行

**对你场景的价值**：
- 移动演示：Medusa Halo可以完整跑130B量化+多Agent+256K上下文
- Strix Halo（现在能买）：72B需量化，4-5个Agent，128K上下文
- 差距：带宽+170%、内存+1倍、Agent数2倍

**来源工具使用**：
- 多工具并行：doubao+kimi+deepseek（5平台）+ minimax + tavily + web_fetch
- subagent：做多轮复杂分析
- 教训：不能只靠1个subagent偷懒，主会话也要并行调用所有工具

### 关键commit（下午）
- `ead33e3` feat(ROBOT-SOP): 应用第一批修改 - RTX5050位宽/BearPi GPIO/Genesis/DGX Spark
- `a1ebcb0` feat(ROBOT-SOP): 应用第三批技术验证 - I2C引脚/vLLM镜像/micro-ROS命令
- `6984b16` feat(SOP): 调研要主动发现新方案，不只是纠错
- `9db2d27` feat(ROBOT-SOP): 补充 GB/T 40013-2021 国标
- `a2293c7` feat(ROBOT-SOP): v3.1 更新 - 版本号/日期/调研记录

### SOP流程确认（重要教训）
1. mdview打开文件后不会自动更新内容——HTML已更新，浏览器需Cmd+Shift+R强制刷新
2. 调研不只是纠错——要主动找更优方案（如GB/T国标、Medusa Halo新发现）
3. 限速期间只开1个subagent，但主会话要直接并行调用所有工具（doubao/kimi/glm/qwen/deepseek + minimax + tavily + web_fetch）
