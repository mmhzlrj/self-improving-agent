# Task T-01-A12 (v2)
{
  "id": "T-01-A12",
  "group": "A",
  "version": 2,
  "type": "atomic-read",
  "line_start": 3301,
  "line_end": 3600,
  "file": "harness/robot/night-build/reference/ROBOT-SOP.md",
  "output_file": "harness/robot/night-build/output/2026-03-26/T-01-A12/result.json"
}

## Prompt

用 read 工具读取文件 `harness/robot/night-build/reference/ROBOT-SOP.md`，参数 offset=3301 limit=299。

**从头到尾逐行扫描返回的内容**，找出所有以 `#` 开头的行（包括 `##`、`###`、`####`）。

对每个标题，记录：
- `line`: 这个标题在 read 返回内容中的第几行（从1开始数）
- `line_in_file`: 用 offset + 行号 - 1 计算（即 3301 + 返回行号 - 1）
- `level`: 几个 # 号（1/2/3/4）
- `heading`: # 号后面的文字（去掉 # 号和空格）

将结果写入 `harness/robot/night-build/output/2026-03-26/T-01-A12/result.json`。

JSON 格式：
```json
{{
  "task_id": "T-01-A12",
  "version": 2,
  "line_start": 3301,
  "line_end": 3600,
  "headings": [
    {{"line_in_return": 5, "line_in_file": 3305, "level": 1, "heading": "标题文字"}}
  ],
  "total_headings": 0
}}
```

**关键规则**：
- 必须逐行扫描，不能跳过任何行
- line_in_file = 3301 + line_in_return - 1
- 如果没找到任何标题，写 "total_headings": 0 和空 headings 数组
- 只写入 JSON 文件，不修改其他文件
