# Task T-01-R01（审核任务 v2：exec + grep 定位 + 审核）
{
  "id": "T-01-R01",
  "group": "R",
  "version": 2,
  "type": "review-with-grep",
  "sections": [
    {"name": "§2.8 技术可行性", "start": 2376, "end": 2404},
    {"name": "§2.7 手部与执行器设计", "start": 2283, "end": 2376},
    {"name": "§2.6 Medusa Halo", "start": 2198, "end": 2283}
  ],
  "output_file": "night-build/output/YYYY-MM-DD/T-01-R01/result.json"
}

## Prompt

你要审核 ROBOT-SOP.md 中的 3 个章节。

**第一步**：用 exec 执行以下命令，精确读取 §2.8 技术可行性（2376-2404行）：

```bash
sed -n '2376,2404p' harness/robot/night-build/reference/ROBOT-SOP.md
```

审核这段内容，找出问题（数据缺失、逻辑矛盾、重复内容、来源缺失、格式问题）。
写入 harness/robot/night-build/output/YYYY-MM-DD/T-01-R01/s2.8.json

**第二步**：用 exec 读取 §2.7 手部与执行器设计（2283-2376行）：

```bash
sed -n '2283,2376p' harness/robot/night-build/reference/ROBOT-SOP.md
```

审核，写入 s2.7.json

**第三步**：用 exec 读取 §2.6 Medusa Halo（2198-2283行）：

```bash
sed -n '2198,2283p' harness/robot/night-build/reference/ROBOT-SOP.md
```

审核，写入 s2.6.json

**第四步**：读取 3 个 section 文件，合并写入 result.json

每个 section 文件格式：
```json
{
  "section": "章节名",
  "line_range": "起-止",
  "issues_found": 0,
  "issues": [
    {"type": "类型", "line_approx": null, "description": "描述", "severity": "high/medium/low"}
  ]
}
```

result.json：
```json
{
  "task_id": "T-01-R01",
  "group": "R",
  "version": 2,
  "total_sections": 3,
  "sections_completed": 0,
  "total_issues": 0,
  "section_results": []
}
```

关键区别（vs v1 BR01）：**用 exec + sed 读取精确行范围，而不是用 read 工具**。
每完成一步必须写入文件再继续下一步。不要编造问题。
