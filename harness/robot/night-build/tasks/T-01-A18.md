# Task T-01-A18: docs.0-1.ai WebMCP 集成

> **ID**: T-01-A18
> **Group**: A（P0 基础能力）
> **Priority**: P0
> **依赖**: 无
> **预计时间**: 15-20 分钟
> **SOP**: `/Users/lr/.openclaw/workspace/harness/robot/night-build/docs-webmcp-sop.md`

## 任务描述

在 docs.0-1.ai 文档站集成 WebMCP（W3C Web Model Context Protocol），让 AI Agent（如 Gemini）可以通过 Chrome 的 `navigator.modelContext` API 直接调用站点功能（搜索文档、读取章节、查询模块状态等），无需 DOM 抓取。

## 强制要求

⚠️ **必须严格按照 SOP 执行，不得跳步或省略！**

SOP 路径: `/Users/lr/.openclaw/workspace/harness/robot/night-build/docs-webmcp-sop.md`

在开始之前，先用 `read` 工具完整读取 SOP 文件（不允许截断！），理解每一步后再执行。

## 执行步骤概览（详见 SOP）

1. **读取 SOP** — `read /Users/lr/.openclaw/workspace/harness/robot/night-build/docs-webmcp-sop.md`
2. **读取源码** — `wc -l` 确认行数，然后完整读取关键区域
3. **添加 WebMCP JS 函数** — 在 `docs-server.py` 中添加 `_make_webmcp_js()` 函数
4. **注入到所有页面** — 在 `</body>` 前注入 WebMCP script
5. **语法检查** — `py_compile` 验证
6. **重启服务** — 杀旧进程，启新进程
7. **功能验证** — curl 验证注入成功、API 正常

## 关键文件

| 文件 | 绝对路径 | 操作 |
|------|---------|------|
| SOP | `/Users/lr/.openclaw/workspace/harness/robot/night-build/docs-webmcp-sop.md` | 只读参考 |
| 源码 | `/Users/lr/.openclaw/workspace/tools/docs-server.py` | **修改** |
| WebMCP 概述 | `/Users/lr/.openclaw/workspace/webmcp.md` | 只读参考 |

## 验证标准

完成后必须通过以下验证：

```bash
# 1. Python 语法正确
python3 -c "import py_compile; py_compile.compile('/Users/lr/.openclaw/workspace/tools/docs-server.py', doraise=True)"

# 2. 服务启动
curl -s -o /dev/null -w "%{http_code}" --max-time 5 http://127.0.0.1:18998/docs.0-1.ai/
# 期望: 200

# 3. WebMCP JS 已注入
curl -s http://127.0.0.1:18998/docs.0-1.ai/ | grep -c "navigator.modelContext"
# 期望: >= 1

# 4. 搜索 API 正常
curl -s http://127.0.0.1:18998/docs.0-1.ai/api/search?q=贵庚 | head -100
# 期望: 返回 JSON 搜索结果

# 5. 多个页面都有 WebMCP
curl -s http://127.0.0.1:18998/docs.0-1.ai/sop/chapter-2.html | grep -c "navigator.modelContext"
# 期望: >= 1
```

## ⚠️ 禁止事项

- ❌ 不得截断读取文件（禁止 head/tail/sed/grep | head）
- ❌ 不得跳过语法检查
- ❌ 不得不验证就报告完成
- ❌ 不得修改 API 端点逻辑（只添加 WebMCP 注册代码）
- ❌ 不得删除或修改现有功能代码

## 完成后输出

完成后汇报：
1. 修改了哪些行（行号范围）
2. 注册了几个工具
3. 验证结果（逐项）
4. 遇到的问题（如果有）
