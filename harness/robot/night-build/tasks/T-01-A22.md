# Task T-01-A22: Step 2 — Multi-agent 记忆共享（Semantic Cache + Namespace）

> **ID**: T-01-A22
> **Group**: A（P0 基础能力）
> **Priority**: P1（依赖 A21）
> **依赖**: T-01-A21（Task 归纳完成）
> **预计时间**: 20-30 分钟
> **SOP**: `harness/robot/night-build/SOP-Self-Memory-3Step.md`（Section 三）

## 前置条件

✅ T-01-A21（Task 归纳）已完成并验证通过。

## 任务目标

扩展 Semantic Cache 支持多层级 namespace：
- `session`（现有，按 session 隔离）
- `user`（新增，跨 session 共享，Task 归纳结果写入这里）
- `project`（新增，按项目隔离）
- `global`（新增，只读全局知识）

## SOP 引用

详细实施步骤见：`SOP-Self-Memory-3Step.md` 的第三节（Step 2: Multi-agent 共享）

**实施前必须完整读取 SOP**。

## 强制规则

⚠️ **遇到不懂的地方，立刻调研。调研无果就记录到 problem-log，然后跳过。**

⚠️ **遇到错误，第一时间记录到日志。完成后同步更新两个 ERRORS.md 并 push。**

## 实施步骤

### Stage 0: 确认收单

确认 A21 已完成，读取 A21 的产出报告。

### Stage 1: 调研

1. **读取 SOP**（完整）
2. **读取 Semantic Cache server.py**（Ubuntu: `~/.semantic_cache/server.py`）
3. **读取 InterestTracker**（参考其跨 session 逻辑，Ubuntu: `~/semantic_cache/server.py`）
4. **确认 Ubuntu 节点在线**（`ping -c 1 192.168.1.18`）

### Stage 2: 实施

**核心改动**：在 Semantic Cache server.py 中增加 namespace 支持。

```python
# 在 query 和 index 函数中增加 namespace 参数
# 详见 SOP Section 三
```

**实施清单**：

1. **修改 Semantic Cache server.py**：
   - 定义 `NAMESPACES` 配置
   - 在 `/query` 端点支持 `namespaces` 参数
   - 在 `/index` 端点支持 `namespace` 参数
   - Task 归纳结果自动写入 `user` namespace

2. **修改 Smart Context Hook**：
   - 查询时增加 `namespaces=['session', 'user']`

3. **验证**：
   - 从 MacBook curl 测试 namespace 查询
   - Task 归纳结果是否出现在 user namespace

### Stage 3: 中途汇报

每完成一个子步骤汇报进度。

### Stage 4: 完成汇报

1. 修改了哪些文件（行号范围）
2. namespace 数量及说明
3. 验证结果（curl 测试输出）
4. 遇到的问题及解决方案
5. 下一步（A23）的依赖关系

## 验证标准

```bash
# 1. Ubuntu 在线
ping -c 1 192.168.1.18

# 2. Semantic Cache 重启成功
curl -s -o /dev/null -w "%{http_code}" http://192.168.1.18:5050/

# 3. 测试 session namespace（默认）
curl -s -X POST http://192.168.1.18:5050/query \
  -H "Content-Type: application/json" \
  -d '{"query": "Jetson Nano", "namespaces": ["session"]}'

# 4. 测试 user namespace
curl -s -X POST http://192.168.1.18:5050/query \
  -H "Content-Type: application/json" \
  -d '{"query": "Phase 0-1", "namespaces": ["user"]}'

# 5. 测试跨 namespace 查询
curl -s -X POST http://192.168.1.18:5050/query \
  -H "Content-Type: application/json" \
  -d '{"query": "Jetson Nano", "namespaces": ["session", "user"]}'
```

## 禁止事项

- ❌ 不得在 A21 未完成时开始 A22
- ❌ 不得截断读取文件
- ❌ 不得跳过错误记录（两边 ERRORS.md 都要）
