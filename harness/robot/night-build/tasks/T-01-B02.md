# Task T-01-B02
{
  "id": "T-01-B02",
  "group": "B",
  "type": "multi-step-v2",
  "file": "night-build/reference/ROBOT-SOP.md",
  "total_lines": 4917,
  "output_file": "night-build/output/2026-03-26/T-01-B02/result.json"
}

## Prompt（v2：带中间检查点）

你的任务是生成文件 `night-build/reference/ROBOT-SOP.md` 的完整标题索引。该文件共 4917 行。

**重要：你必须逐段读取，每读一段立即写入中间结果，不能跳过任何一段。**

执行步骤：

**第一步**：读取第 1-500 行。找出所有 `#` 开头的标题（含 `##` `###` `####`）。
→ 将这一段的标题列表写入文件 `night-build/output/2026-03-26/T-01-B02/segment-01.json`

**第二步**：读取第 501-1000 行。找标题。
→ 写入 `segment-02.json`

**第三步**：读取第 1001-1500 行。找标题。
→ 写入 `segment-03.json`

**第四步**：读取第 1501-2000 行。找标题。
→ 写入 `segment-04.json`

**第五步**：读取第 2001-2500 行。找标题。
→ 写入 `segment-05.json`

**第六步**：读取第 2501-3000 行。找标题。
→ 写入 `segment-06.json`

**第七步**：读取第 3001-3500 行。找标题。
→ 写入 `segment-07.json`

**第八步**：读取第 3501-4000 行。找标题。
→ 写入 `segment-08.json`

**第九步**：读取第 4001-4500 行。找标题。
→ 写入 `segment-09.json`

**第十步**：读取第 4501-4917 行。找标题。
→ 写入 `segment-10.json`

**最终**：读取所有 10 个 segment 文件，合并为一个完整列表，写入 `result.json`。

每个 segment 和 result.json 的格式：
```json
{
  "segment": 1,
  "line_start": 1,
  "line_end": 500,
  "headings": [
    {"line": 1, "level": 1, "heading": "标题文字"}
  ]
}
```

result.json 额外包含：
```json
{
  "task_id": "T-01-B02",
  "group": "B",
  "prompt_version": "v2-checkpoint",
  "total_headings": 0,
  "segments_completed": 0,
  "headings": []
}
```

**关键**：每完成一步必须写入文件再继续下一步。如果你发现自己在写 result.json 但没有完成所有 10 段的读取，停下来，记录已完成到哪一步。
